local validKey = "Yuichi-vip1"

function validateKey(userKey)
    return userKey == validKey
end

local userKey = gg.prompt({"Enter your key:"}, {""}, {"text"})[1]
if validateKey(userKey) then
    gg.alert("Access granted!")
    -- Add your script functionality here
else
    gg.alert("Invalid key!")
end
